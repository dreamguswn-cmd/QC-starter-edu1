# 성능 테스트 보고서

## 1. 조건
- 도구: k6 (`performance/k6_test.js`)
- 부하: 30s 워밍업(10 VU) → 1m 유지(30 VU) → 30s 정리
- 대상: POST /ask · 판정 기준: **오류율 5% 이하 AND p95 1000ms 이하**

## 2. 결과 기록 (실행 후 기입)
| 모드 | 총 요청 | 오류율 | 평균(ms) | p95(ms) | 판정 |
| --- | --- | --- | --- | --- | --- |
| rule | ___ | ___% | ___ | ___ | Pass/Fail |
| api  | ___ | ___% | ___ | ___ | Pass/Fail |

## 3. 예상 특성과 해석 포인트
- rule 모드: 순수 파이썬 매칭 → ms 단위 응답, p95 기준 여유 예상
- api 모드: 외부 LLM 호출 → 초 단위 지연, p95 1000ms 기준 초과 가능성 높음
  → 초과 시 개선안: 캐싱, rule 우선 하이브리드 라우팅, 타임아웃·재시도 정책
- 1차 "품질 비교"에 이은 "성능 비교" — 동일 질문 세트로 모드만 바꿔 측정

## 4. 모니터링 연계
k6 실행 중 Grafana에서 요청량·p95·오류율 그래프가 실시간 반영되는지 확인 (테스트 수준 6)
